os:
    - linux

language: julia

julia:
    - 1.0
    - nightly

notifications: 
    email: false

git:
    depth: 99999


jobs:
  include:
    - stage: deploy
      julia: 1.0
      os: linux
      script:
        - julia --color=yes --project=docs/ -e 'using Pkg; Pkg.instantiate()'
        - julia --color=yes --project=docs/ docs/make.jl
after_success: skip

